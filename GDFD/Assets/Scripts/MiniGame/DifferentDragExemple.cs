using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DifferentDragExemple 
{

    //transform.localPosition = PosInCanvase(eventData.position);
    //transform.localPosition = PosInCanvase(eventData.pointerCurrentRaycast.screenPosition);
    //transform.localPosition = Input.mousePosition;
    //transform.localPosition =eventData.position;
    //transform.GetComponent<RectTransform>().anchoredPosition = Input.GetTouch(0).position;
    //transform.GetComponent<RectTransform>().anchoredPosition = Input.mousePosition;


}
